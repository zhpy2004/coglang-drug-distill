# Training / data-engine stack (Python 3.12, CUDA 12.4 build of torch).
# Install torch from the cu124 index, e.g.:
#   pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
transformers==5.9.0
peft==0.19.1
bitsandbytes==0.49.2
accelerate==1.13.0
trl==1.5.1
datasets==4.8.5
numpy==2.4.6

# Teacher (data engine) — any OpenAI-compatible endpoint (DeepSeek used here)
openai==2.34.0

# GGUF conversion / quantization helpers
gguf==0.19.0
sentencepiece==0.2.1
protobuf==4.25.9

# The CogLang language + verifier (parser, executor, generation-eval).
# Install the upstream package separately:
#   pip install coglang
# or from source: https://github.com/jaysinailabs/coglang
